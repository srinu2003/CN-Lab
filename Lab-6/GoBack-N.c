#include<stdio.h>
#include<stdlib.h>

// Global variables
int N = 10;       // Window size
int Rn = 0;       // Request number
int Sn = 0;       // Sequence number
int Sb = 0;       // Sequence base
int Sm = 10 + 1;  // Sequence max

int main(){
    int n, ws, frame_size, sender_frame,receiver_frame;
    printf("Enter no.of frames: ");
    scanf("%d",&frame_size);
    printf("Enter window size: ");
    scanf("%d",N);
    printf("N value: ",N);
    // for(int i = 0; i < n; i++){
        
    // }
}